apiVersion: v1
kind: Service
metadata:
  name: hello-world-app-svc
  labels:
    app: hello-world-app
spec:
  selector:
    app: hello-world-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
